<div class="container">
	<div class="jumbotron">
		<%= render 'form' %>
		<%= link_to '<i class="fas fa-undo"></i> Back'.html_safe, blog_post_path(@comment.post.blog, @comment.post), class: "btn btn-dark btn-sm" %>
	</div>
</div>