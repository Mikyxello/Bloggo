<div class="container-fluid" style="margin-top:10px; margin-bottom:10px;">
  <div class="row container-fluid">
    <div class="col" style="max-width:60%; max-height:50%;">
      <div class="card">
        <div class="card-header pastel_colour_blue" style="text-align:center;">
          <h2>Top Trending Bloggoes</h2>
        </div>
        <% if @most_viewed != nil && @most_viewed.count != 0%>
          <%= render @most_viewed %>
        <% else %>
        <div class="card-body">
          <h3> No bloggoes found! </h3>
        </div>
        <% end %>
      </div>
    </div>

    <div class="col" style="max-width:40%; max-height:50%;">
      <div class="card">
      <div class="card-header pastel_colour_yellow" style="text-align:center; ">
        <h2>Top Trending Taggoes</h2>
      </div>
      <div class="card-body">
        <% if @tags != nil && @tags.count != 0%>
          <%= render @tags %>
        <% else %>
          <h3> No taggoes found! </h3>
        <% end %>
      </div>
    </div>
  </div>

    <div class="w-100"></div>

    <div class="col" style="margin-top:10px; max-width:40%; max-height:50%;">
      <div class="card">
        <div class="card-header pastel_colour_red" style="text-align:center;">
          <h2>Bloggoes Showcase</h2>
        </div>
        <% if @blogs != nil && @blogs.count != 0 %>
          <%= render @blogs %>
          <% else %>
          <div class="card-body">
            <h3> No bloggoes found! </h3>
          </div>
          <% end %>
      </table>
      </div>
    </div>

    <div class="col" style="margin-top:10px; max-width:60%; max-height:50%;">
      <div class="card">
          <div class="card-header pastel_colour_green" style="text-align:center;">
            <h2>Top Trending Postoes</h2>
          </div>
            <% if @posts != nil && @posts.count != 0 %>
              <%= render @posts %>
            <% else %>
              <div class="card-body">
                <h3> No postoes found</h3>
              </div>
            <% end %>
          </div>
    </div>
  </div>
</div>

<!-- <div class="card" style="width: 50rem; height:20rem;">
  <div class="card-header bg-success" style="text-align:center;">
    <h2>Featured Posts</h2>
  </div>
  <% if @posts != nil && @posts.count != 0%>
    <table class="table">
      <tr>
        <th scope="col">Title</th>
        <th scope="col">Image</th>
        <th scope="col">Author</th>
        <th scope="col" colspan="3">Blog</th>
      </tr>
    <% @posts.each do |post| %>
      <tr>
        <td><%= link_to post.title, blog_posts_path(post) %></td>
        <td>
          <%= image_tag(post.file.url, alt: post.file.identifier, width:'40px',height:'40px') if post.file? %>

        </td>
        <td><%= link_to post.user.name, user_path(post.user) %></td>
        <td><%= link_to post.blog.name, blog_path(post.blog) %>
        </td>
      </tr>
    <% end %>
  <% else %>
    <h3> No blogs found! </h3>
  <% end %>
</table>
</div> -->
